<form method="post" action="{{ path('app_battle_delete', {'id': battle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ battle.id) }}">
    <button class="btn">Delete</button>
</form>
